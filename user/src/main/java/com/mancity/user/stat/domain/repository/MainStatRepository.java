package com.mancity.user.stat.domain.repository;

import com.mancity.user.stat.domain.MainStat;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MainStatRepository extends JpaRepository<MainStat, Long> {
}

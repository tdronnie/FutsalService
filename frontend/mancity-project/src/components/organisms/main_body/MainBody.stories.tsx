import MainBody from "./MainBody";

export default {
  title: "components/organisms/MainBody",
  component: MainBody,
};

const Template = () => <MainBody />;

export const Default = Template.bind({});

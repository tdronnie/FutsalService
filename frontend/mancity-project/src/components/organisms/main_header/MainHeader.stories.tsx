import MainHeader from "./MainHeader";

export default {
  title: "components/organisms/MainHeader",
  component: MainHeader,
};

const Template = () => <MainHeader />;

export const Default = Template.bind({});

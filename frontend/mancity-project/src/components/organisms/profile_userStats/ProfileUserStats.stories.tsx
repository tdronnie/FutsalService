import ProfileUserStats from "./ProfileUserStats";

export default {
  title: "components/organisms/ProfileUserStats",
  component: ProfileUserStats,
};

const Template = () => <ProfileUserStats />;

export const Default = Template.bind({});

import ProfileUserInfo from "./ProfileUserInfo";

export default {
  title: "components/organisms/ProfileUserInfo",
  component: ProfileUserInfo,
};

const Template = () => <ProfileUserInfo />;

export const Default = Template.bind({});

interface GlobalType {
  test: string;
}

interface NavigateType {
  path: string;
}

interface MatchData {
  id: number;
  file: string;
  subtext: string;
  maintext: string;
  minitext: string;
  buttonlabel: string;
}
